extends ../layout/common
block content
    figure#hero3.hero.mt-5
            .inner
                .copy
                    h1.shadow UserList
                        span.red .
                    p.text-white.shadow Lorem ipsum dolor sit amet, consectetur adipiscing elit. Nullam a ultricies metus. Sed nec molestie eros. Sed viverra velit venenatis fermentum luctus.
    .container
        .d-flex.align-items-center
            a.btn.btn-primary#createUser(href="/contact/create") Create New User
            form.ml-auto(action="/contact/search",method="GET",class="form-group d-flex")
                input(type="text", name="q",class="form-control" )
                button(class="btn btn-info ml-2") Search
        table.table.mb-5
            tr
                th Avatar
                th Name
                th Phone
                th Actions
            each user in users
                tr
                    td
                        img(src=`${user.avatar}`, width="64")
                    td= user.name
                    td 0#{user.phone}
                    td
                        a.btn.btn-success#viewUser(href=`/contact/${user.id}`) View
                        a.btn.btn-success.ml-2#editUser(href=`/contact/${user.id}/update`) Edit
                        a.btn.btn-danger.ml-2#removeUser(href=`/contact/delete/${user.id}` onclick="return confirm('Are you sure you want to delete?');") Delete 